package me.sshcrack.tinkerers_fletching.client;

import net.minecraft.util.Identifier;

public interface DrawContextDuck {
    void tinkerers$drawGuiTexture(Identifier texture, int x, int y, int width, int height);
    void tinkerers$drawGuiTexture(Identifier texture, int x, int y, int z, int width, int height);
}
